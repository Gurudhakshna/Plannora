"""
Database query layer package.

Provides reusable CRUD and search functions for the database layer.
All query functions enforce user ownership to ensure data isolation.

Modules:
    document_chunks — CRUD and search for document chunks
    chat_messages   — CRUD for chat conversation history
    analytics       — Aggregation and statistics queries
    search          — Text and vector similarity search
"""
